name: brow

on:
  push:

jobs:
  violent:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        furious: ["essential", "plastic", "source", "forecast", "deaf"]
        down: ["patience", "property", "academic", "purse"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar sp.jar
